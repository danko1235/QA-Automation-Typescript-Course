export interface ImageResponse {
    id: string;
    url: string;
}

export interface VoteResponse {
    id: string;
    message: string;
}

export interface FavouriteResponse {
    id: string;
    image_id: string;
}
